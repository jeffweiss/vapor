defmodule Vapor.ConfigTest do
  use ExUnit.Case, async: true

  test "config layers multiple sources" do
    [{source, version, values}
     {source2, version, values}]
  end

  test "users can get config by passing in key paths" do
  end

  describe "source versions" do
    test "only the most recent version is used" do

    end
  end
end
